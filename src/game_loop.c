#include "../inc/cub3d.h"

// Esta función la llama mlx_loop_hook() en cada frame
// Por ahora solo limpia la pantalla con negro — aquí pondrás el raycasting
int	game_loop(t_map *data)
{
	// TODO: llamar a handle_keys() para mover el jugador
	// TODO: llamar a render_frame() para dibujar el frame
	// Por ahora, solo para que compile:
	(void)data;
	return (0);
}
